%h1 Registrations

%p
  Download:
  = link_to "Excel", registrations_path(format: "xls")

%table.table.table-striped.table-bordered.table-hover.table-condensed{:id => :registrations}
  %thead
    %tr
      %th Registration #
      %th Player
      %th Email
      %th Grade
      %th Age
      %th Gender
      %th Priority
      %th Volunteer Type
      %th Practice Days
      %th Lives In District?
      %th Has GE?
      %th
      %th

  %tbody
  - @registrations.each do |registration|
    %tr
      %td= link_to(registration.number, registration)
      %td= registration.player_name
      %td= registration.email_address
      %td= registration.grade
      %td= registration.age
      %td= registration.gender
      %td= registration.priority
      %td= registration.volunteer_type
      %td= format_practice_days(registration.practice_days)
      %td= registration.lives_in_district
      %td= registration.has_geographic_exception
      %td= link_to 'Edit', edit_registration_path(registration)
      %td= link_to 'Destroy', registration, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Registration', new_registration_path
